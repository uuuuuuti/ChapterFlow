/**
 * Host face for the ChapterFlow client package.
 *
 * Browser behavior is exported from ./client and loaded by Harness through the
 * package.json dsh.client declaration.
 */
export function apply(): void {}
