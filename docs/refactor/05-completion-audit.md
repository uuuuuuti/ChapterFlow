# Phase 0 / 1 / 2 / 4 完成度复核

复核日期：2026-09-08。依据原始附件与 `docs/REFACTOR_MASTER_PLAN.md`。前次总体验收通过不代表每项产品语义均已完成。

## 当前证据

- Phase 0：已记录上游提交、fork、独立分支、架构、截图和基线测试。
- Phase 1：API 分模块、兼容 barrel、草稿 hook、助手会话选择器及连续创作状态逻辑已抽出；仍需核查 Query Key 统一与组件边界是否满足方案范围。
- Phase 2：新路由、作品库、创作首页、导航、浅色设计系统和旧工作区共存已实现，并有四视口测试。
- Phase 4：手工保存、重试、版本恢复、章纲、AI 建议采纳和任务显示已实现；本次补齐检查问题卡片中的“生成修改建议”，复用选区改写候选接口，以整章作为修改范围，仍需作者接受。
- 新增检查非空问题的 E2E：检查 → 问题卡片 → 生成修改建议 → 正文保持不变 → 接受 → 刷新后正文保留。2026-09-08 四视口专项运行结果为 passed（`.tmp/playwright-results/.last-run.json`）。
- 本次 `npm run verify` 完整通过：117 个文件、691 项测试、证据协议、许可证与构建。发现并修正前次提交中源码链接已更新为 ChapterFlow、测试仍断言上游地址的遗漏；保留其新窗口和链接属性验证。日志：`/tmp/chapterflow-audit-verify.log`。

## 尚不能证明完成的要求

1. **续写语义（本次补齐）**：写作台提交保护版本 ID，服务端确认该版本仍为目标章节当前版本，并读取其原文作为持久化续写前缀。复用现有 Narrative 追加生成逻辑，候选正文为原文加新增内容；作者接受前不写入正式正文。续写任务禁用会改写整章的自动修订循环，检查仍保留。四视口 AI E2E 新增原文前缀断言并通过（`/tmp/chapterflow-continuation-e2e.log`）。新增 API 用例验证旧版本请求被拒绝、前缀来自服务端保存版本。API 为可选字段增量扩展，无数据库或 Domain Model 改动。
2. **选区操作完整性（本次补齐）**：新增“插入到选区后”。采纳接口可选 `mode: insert_after`，默认继续替换；服务层在原事务及基础版本冲突校验后拼接正文并追加版本。未修改数据库或 Domain Model。四视口 E2E 已证明保留选区及后文、采纳前不变、插入后刷新持久。日志：`/tmp/chapterflow-insert-e2e.log`（4 passed）。
3. **版本与并发保护**：已覆盖普通离页和保存失败；需继续核查浏览器后退、任务采纳与编辑同时发生时的保护边界。
4. **逐项验收证据**：仍需核对方案中的全部阶段交付物、公共组件边界和普通模式用语，完成正式逐项审计后才能声明目标完成。

Phase 3、5、6、7 不在用户指定执行阶段内；其专属新能力不列为本轮缺项。
